# 路线图

`neo-runner` 的演进将分阶段推进，优先保证可用性，再逐步提升编排能力与生态完整性。

## 阶段一：MVP 可用版本

目标是打通本地可执行闭环：支持 `shell/http` 基础任务、重试与超时策略、最小化报告输出。

## 阶段二：调度与编排增强

引入 DAG 依赖调度、`fail-fast` 控制、并发队列与 matrix 扩展，以支持更复杂的流水线任务场景。

## 阶段三：插件与工程化完善

完成插件接口稳定化，补齐发布自动化与安全检查流程，形成可持续维护的版本发布与质量保障机制。
